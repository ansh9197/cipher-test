name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: pip install nonexistent-package-xyz123
      - name: Run tests
        run: python -m pytest tests/
